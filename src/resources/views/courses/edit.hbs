<div class='mt-4'>
    <h3>Cập nhật khóa học</h3>
    {{! Chú ý attr name trong form phải trùng với field trong database (name thôi) nếu k sẽ lỗi k tạo được đầy đủ thuộc tính }}
    {{! req.body.name của input nào thì sẽ nhận đc value của input đó }}
    {{! Ở đây dùng put(replace luôn)/patch(chỉ sửa những field đc điền), tuy nhiên form html chưa có tính năng này }}
    {{! mà nó sẽ như get và query hết trên đường dẫn(xem video để thấy) => dùng method-override: http://expressjs.com/en/resources/middleware/method-override.html }}
    <form method='post' action='/courses/{{course._id}}?_method=PUT'>
        <div class='form-group'>
            <label for='name'>Tên khóa học</label>
            <input type='text' class='form-control' value='{{course.name}}' id='name' name='name' />
        </div>
        <div class='form-group'>
            <label for='description'>Mô tả khóa học</label>
            {{! desc dùng textarea hợp lý hơn vì nó điền được nhiều, nhớ thẻ đóng vì input k cần còn nó có }}
            {{! chú ý paste value vào giữa cặp mở và đóng, vì set value như input k đc đâu }}
            <textarea type='text' class='form-control' id='description' name='desc'>{{course.desc}}</textarea>
        </div>
        <div class='form-group'>
            <label for='videoId'>Video ID khóa học</label>
            <input type='text' class='form-control' value='{{course.videoId}}' id='videoId' name='videoId' />
        </div>
        <div class='form-group'>
            <label for='level'>Level khóa học</label>
            <input type='text' class='form-control' value='{{course.level}}' id='level' name='level' />
        </div>

        <button type='submit' class='btn btn-primary'>Lưu lại</button>
    </form>
</div>